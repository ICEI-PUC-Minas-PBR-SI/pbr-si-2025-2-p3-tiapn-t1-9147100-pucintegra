// db.js
const mysql = require('mysql2/promise');

const pool = mysql.createPool({
    host: 'localhost', // Endereço do seu servidor MySQL
    user: 'root',   // << SUBSTITUIR
    password: '12345', // << SUBSTITUIR
    database: 'puc_integra', // Nome do banco
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
});

module.exports = pool;